# machinelearn_phases.jl

This package sets up a simple neural network, which can be trained on samples of an Ising model (using [ising_mc](https://github.com/ELW/ising_mc)) at two temperatures and can then be used to predict the phases at all other temperatures.